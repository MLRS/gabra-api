extends layout

block content
  
  h3 #{title}

  div#editor

  script(src=baseURL+'/module/@json-editor/json-editor/dist/jsoneditor.js')
  script(src=baseURL+'/javascripts/jsoneditor-defaults.js')
  script.
    var editor_element = document.getElementById('editor')
    var editor = new JSONEditor(editor_element, {
      schema: !{JSON.stringify(schema)}
    })
    
